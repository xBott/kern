package me.bottdev.kern.commons.diagnostic;

/// Type of the diagnostic record: **INFO**, **WARN**, **ERROR**.
/// All diagnostics are grouped by their type.
public enum DiagnosticType {
    INFO,
    WARN,
    ERROR
}
